# Install at least one provider SDK (Anthropic is the default backend).
# The openai package also powers the local Ollama provider.
anthropic>=0.40.0
openai>=1.0.0
google-genai>=1.0.0
python-dotenv>=1.0.0
